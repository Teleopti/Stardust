<UserControl x:Class="Teleopti.Ccc.Win.WpfControls.Controls.Messaging.Views.FollowUpDialogueMessageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:Teleopti.Ccc.Win.WpfControls.Controls">
	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="35"/>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
        <controls:AnalogPresenter DataContext="{Binding Path=CreatedAsLocal}"  
								  HorizontalAlignment="Stretch" 
								  VerticalAlignment="Center"
								  Margin="0"/>

        <TextBlock Text="{Binding Path=CreatedAsLocal,StringFormat=d}"
				   Grid.Column="1"  
				   Margin="2,0,5,0" 
				   VerticalAlignment="Center"/>
		
		<TextBlock	Grid.Column="2"
					Text="{Binding Path=Text}" 
					TextWrapping="Wrap"
					VerticalAlignment="Center"/>
	</Grid>
</UserControl>
