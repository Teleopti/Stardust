<UserControl x:Class="Teleopti.Ccc.WpfControls.Controls.Intraday.SimpleLayerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Converters="clr-namespace:Teleopti.Ccc.WpfControls.Converters"
    xmlns:WpfControls="clr-namespace:Teleopti.Ccc.WpfControls"
    Height="Auto" 
    Width="Auto"
    ToolTipService.InitialShowDelay="500"
    ToolTipService.ShowDuration="7000"
    ToolTipService.BetweenShowDelay="2500">

    <UserControl.Resources>
        <Converters:ColorConverter x:Key="ColorConverter"/>
        <DataTemplate x:Key="DescriptiveTemplate">
            <WpfControls:LayerDescriptiveView DataContext="{Binding}"/>
        </DataTemplate>
    </UserControl.Resources>

   
    <Border HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            Opacity="1"
            Background="{Binding Path=DisplayColor, Converter={StaticResource ColorConverter}, FallbackValue=White}"
            Cursor="Hand"
            MouseLeftButtonDown="Border_MouseDown"/>

    <UserControl.ToolTip>
        <ToolTip Opacity="0.8">
            <ContentControl Content="{Binding}" ContentTemplate="{StaticResource DescriptiveTemplate}" />
        </ToolTip>
    </UserControl.ToolTip>

</UserControl>
